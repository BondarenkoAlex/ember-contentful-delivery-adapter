import Ember from 'ember';

export default Ember.Route.extend({
  model() {
    //return this.store.findRecord('content-type', 'commentPost');
    //return this.store.query( 'content-type' , {} );
  }
});
